import { Locator, Page, expect } from "@playwright/test";
import { ComponentMapper } from "../helpers/componentMapper";



type headerMappings = {
};

export default class AlertsTableComponent {
  
  readonly page: Page;


  constructor(page: Page) {
    this.page = page;

    const headerMappings: headerMappings = {
     
    };

  }

}
